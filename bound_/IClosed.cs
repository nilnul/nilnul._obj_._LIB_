namespace nilnul.obj.bound_
{
	/// <summary>
	/// if we have a comp, then we can define a closed bound no matter whether the type is finite, close, bounded, dense, continuous.
	/// 
	/// </summary>
	public interface IClosed:IBound { }
}

namespace nilnul.obj.rec
{
	/// <summary>
	/// how many components in the rec
	/// </summary>
	/// alias:
	///		arity
	///		count
	///		length
	///			confusiong with <see cref="nilnul.geometry.linear.tend.IMeasure"/>, which is called length, or distance, or if from origin, departure; <seealso cref="geometry.linear.grad.ISignedMeasure"/>, which is called deviation if from origin, or displace in general
	///		size
	///			which is arity*arity, not area
	///		
	///		
	public interface IArity
	{
	}
}
namespace nilnul.obj
{
	/// <see cref="identy.ITagged"/>
	/// <summary>
	/// extend <see cref="IIdentifier"/> to accomodate:
	///		nilnul.obj.1.2.3-prerelease
	/// </summary>
	/// defer the implementation of this to <see cref="nilnul.txt"/> or <see cref="nilnul.dev"/> where the use case is available, and thus implementable with the context/application in mind.
	///

	public interface IIdentification {

	}

	/// ,or,
	/// contexted <see cref="IIdenty"/> ?
	/// no: as id, identy, identifier is affine to each other, and all refers the object itself without context.
	/// For contexted, the mechanism shall be defined independent from this; for example, we can define that as:
	///		spec
	///		meta
	///		info
	///		data


}


namespace nilnul.obj.stream_
{
	/// <summary>
	/// convert each term of the underlying stream before outputting it as the term of the wrapping stream;
	/// </summary>
	/// alias:
	///		select
	///		sel
	///		func
	///		termwise
	///		map
	public interface IMap :IStream{ }


}

namespace nilnul.obj.stream
{
	/// <summary>
	/// a stream of strings; the str first is empty, then contain one element, then the next two;
	/// </summary>
	/// alias:
	///		stow
	///		store
	///		
	public interface IStock { }
}

namespace nilnul.obj.betwixt_
{
	/// <summary>
	/// if we have a comp, then we can define a closed range no matter whether the type is finite, close, bounded, dense, continuous.
	/// 
	/// </summary>
	public interface IClosed:IBetwixt
		,
		ISymmetric
	{ }
}

namespace nilnul.obj.str_.started
{

	/// <summary>
	/// except <see cref="IToe"/>;
	/// </summary>
	/// vs:
	///		<see cref="ITop"/>, which is from head to somewhere, whileas this is from head to second from last.
	/// alias:
	///		detoe
	///		trunk
	///			not trunc, which is the short form for truncate;
	///			like truncate, which is to detoe;
	public interface ITrunk { }
}
